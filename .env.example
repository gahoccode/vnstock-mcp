# VNStock MCP Server Configuration

# Set PORT to enable HTTP transport (for Render/cloud deployment)
# Leave unset for local STDIO transport (uvx usage)
# PORT=8001

# PyPI publishing token (for maintainers only)
# UV_PUBLISH_TOKEN=your-pypi-token
